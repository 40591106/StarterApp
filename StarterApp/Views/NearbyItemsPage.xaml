<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
  x:Class="StarterApp.Views.NearbyItemsPage"
  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:vm="clr-namespace:StarterApp.ViewModels"
  xmlns:models="clr-namespace:StarterApp.Database.Models;assembly=StarterApp.Database"
  x:DataType="vm:NearbyItemsViewModel"
  Title="Items">

  <Grid RowDefinitions="Auto,Auto,Auto,*"
        Padding="20"
        RowSpacing="10">

    <!-- Radius Slider -->
    <Label Grid.Row="0"
           Text="Search Radius"
           FontSize="16"
           FontAttributes="Bold"/>
    <StackLayout Grid.Row="1"
                 Orientation="Horizontal"
                 Spacing="10">
      <Slider Minimum="1"
              Maximum="50"
              Value="{Binding RadiusKm}"
              HorizontalOptions="FillAndExpand"
              MinimumTrackColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"/>
      <Label Text="{Binding RadiusKm, StringFormat='{0:F0} km'}"
             VerticalOptions="Center"
             FontSize="16"
             FontAttributes="Bold"
             WidthRequest="50"/>
    </StackLayout>

    <!-- Find Button / Status -->
    <StackLayout Grid.Row="2"
                 Spacing="5">
      <Button Text="Find Nearby Items"
              Command="{Binding FindNearbyCommand}"
              BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
              TextColor="White"/>
      <ActivityIndicator IsRunning="{Binding IsLoading}"
                         IsVisible="{Binding IsLoading}"
                         HorizontalOptions="Center"/>
      <Label Text="{Binding LocationStatus}"
             HorizontalOptions="Center"
             FontSize="14"
             TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
      <Label Text="{Binding ErrorMessage}"
             IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}"
             TextColor="Red"
             HorizontalOptions="Center"/>
    </StackLayout>

    <!-- Items List -->
    <CollectionView Grid.Row="3"
                    ItemsSource="{Binding Items}">
      <CollectionView.EmptyView>
        <Label Text="No items found nearby. Try increasing the radius."
               HorizontalOptions="Center"
               VerticalOptions="Center"
               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
      </CollectionView.EmptyView>
      <CollectionView.ItemTemplate>
        <DataTemplate x:DataType="models:Item">
          <Border BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
                  Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                  StrokeThickness="1"
                  Padding="20"
                  Margin="0,0,0,10">
            <Border.StrokeShape>
              <RoundRectangle CornerRadius="12"/>
            </Border.StrokeShape>
            <Border.GestureRecognizers>
              <TapGestureRecognizer
                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:NearbyItemsViewModel}}, Path=NavigateToDetailCommand}"
                CommandParameter="{Binding Id}"/>
            </Border.GestureRecognizers>
            <Grid ColumnDefinitions="*,Auto">
              <StackLayout Grid.Column="0">
                <Label Text="{Binding Title}"
                       FontSize="16"
                       FontAttributes="Bold"/>
                <Label Text="{Binding Category}"
                       FontSize="12"
                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
                <Label Text="{Binding Distance, StringFormat='{0:F1} km away'}"
                       FontSize="12"
                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"/>
              </StackLayout>
              <Label Grid.Column="1"
                     Text="{Binding DailyRate, StringFormat='£{0:F2}/day'}"
                     FontSize="14"
                     FontAttributes="Bold"
                     VerticalOptions="Center"
                     TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"/>
            </Grid>
          </Border>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>
  </Grid>
</ContentPage>
